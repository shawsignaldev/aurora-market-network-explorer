# Example Analysis

A correlation threshold controls how dense the network becomes. Lower thresholds show broad relationships, while higher thresholds isolate only the strongest asset relationships.

Future versions should add sector metadata, rolling-window analysis, and exportable reports.

